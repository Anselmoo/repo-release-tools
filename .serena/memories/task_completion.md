When code changes are made, validate with the repo's existing checks: lint via `uvx pre-commit run --all-files`, run fast tests with `uv run pytest -q -m "not runtime"`, and run runtime integration tests when relevant. Prefer dry-run first for mutating `rrt` commands because release and version operations support `--dry-run`. Update directly related docs when behavior or setup changes.
